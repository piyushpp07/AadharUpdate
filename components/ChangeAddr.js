import React from 'react';
import { View, StyleSheet, Text } from 'react-native';

const ChangeAddr = () => {
   return (
      <View>
         <Text>This is change Address</Text>
      </View>
   );
}

const styles = StyleSheet.create({})

export default ChangeAddr;
